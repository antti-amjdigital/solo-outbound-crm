/** Weekly dial target shown on the Call activity chart. */
export const WEEKLY_DIAL_TARGET = 30

export type FunnelStages = {
  enrolled: number
  contacted: number
  connected: number
  meetings: number
}
